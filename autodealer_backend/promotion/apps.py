from django.apps import AppConfig


class PromotionConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "autodealer_backend.promotion"
    verbose_name = "Promotion"
    label = "promotion"
